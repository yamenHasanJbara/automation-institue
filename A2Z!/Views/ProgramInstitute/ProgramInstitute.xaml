<Page x:Class="A2Z_.Views.ProgramInstitute.ProgramInstitute"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
      xmlns:local="clr-namespace:A2Z_.Views.ProgramInstitute"
      mc:Ignorable="d" 
      Title="تعديل مدة الكورس"
      FlowDirection="RightToLeft">
    <Grid>
        <TextBlock  Text="تعديل المدة" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="40" Foreground="Gray" FontSize="18"/>
        <StackPanel VerticalAlignment="Top" Margin="60">
            <TextBox x:Name="StartTime" materialDesign:HintAssist.Hint="بداية الدوام" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,10" FontFamily="Champagne &amp; Limousines" FontSize="18" />
            <TextBox x:Name="CourseLength" materialDesign:HintAssist.Hint="مدة الكورس" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,10" FontFamily="Champagne &amp; Limousines" FontSize="18" />
            <Button Click="Update" Foreground="White" VerticalAlignment="Bottom" Margin="0,50" Width="150" Content="تعديل"  Style="{StaticResource MaterialDesignFlatAccentBgButton}" Background="Black" Opacity="0.5" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Page>
